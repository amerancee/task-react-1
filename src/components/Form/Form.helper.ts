export const validateTodo = (todo: FormDataEntryValue | null) => {
    // тут нужно написать логику валидации, которая должна проверить:
    // 1) todo является string
    // 2) длина строки от 5 до 20 символов
    // если оба условия соблюдены - возвращается true, иначе - false
    return true
}